# Development: Vite loads this at dev server startup
VITE_API_URL=http://localhost:8000

# Production: Docker container uses API_URL environment variable at runtime
# This allows the API URL to be configured without rebuilding the image
# Example: docker run -e API_URL=https://api.production.com -p 8080:80 frontend
